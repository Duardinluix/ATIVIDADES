#include <stdio.h>

    int somar (int a, int b) {
        int resultado= a + b;
        return resultado ;
    
}

int main (){
    int soma1= 10;
    int soma2= 12;
    int soma = somar ( soma1, soma2);
    printf ("a soma de %d e %d é %d\n" , soma1, soma2, soma);
    return 0;
}