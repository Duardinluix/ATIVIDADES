#include <stdio.h>

   char habilidade (){
       char aceitar = 's' ;
       
       while (aceitar == 's'){
           
           printf ("deseja usar habilidade\n");
           scanf ("%s", &aceitar);
           printf ("você usou habilidade!");
           
       }
        return aceitar;
 }
int main ()
{
    habilidade ();
    return 0;
}